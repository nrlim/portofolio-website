/**
 * Feature Flags Configuration
 * Easily toggle features on/off without modifying component code
 */

export const featureFlags = {
  // Services menu visibility - toggle when ready
  SHOW_SERVICES_MENU: false,
} as const;
