version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nuralim_portfolio
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - postgres-network

networks:
  postgres-network:
    external: true
